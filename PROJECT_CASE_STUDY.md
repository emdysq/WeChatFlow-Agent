# WeChatFlow 项目案例与面试说明

## 1. 一句话介绍

WeChatFlow 是我基于 MIT 开源项目 WeWrite 进行二次开发的微信公众号 Agent 内容工作流，
重点补齐 Windows 交付、可审阅改稿和可验证的微信草稿链路。

## 2. 为什么没有从零开发

真实目标是尽快得到能运行、能演示、能解释的求职项目，而不是重复实现 Markdown 转换、
主题系统和微信 API 封装。评估后选择 WeWrite 作为基座，保留其成熟内容管道，把时间投入到
与实际公众号工作流更相关、也更能体现产品与工程判断的差异化部分。

面试时应主动说明：这是开源二次开发，不是从零独立开发。

## 3. 用户问题

在公众号运营实践中，选题、资料核对、写作、反复改稿、排版、素材上传和草稿同步分散在
多个工具里。重复操作多，AI 修改不容易审阅，远程写入也缺少明确的执行前检查。

## 4. 我的工作

### 发布链路

- 增加发布 Dry-run，提前展示标题、摘要长度、正文长度、主题、图片动作、兼容性问题和阻断项。
- 将任务成稿、审稿结果和远程草稿创建串成可检查的运行状态。
- 使用 Mock 覆盖 token、正文图片、永久封面和 `draft/add` 的请求契约。
- 在用户明确授权后完成真实公众号 Canary；后台人工确认标题、正文排版和封面正常。
- 项目只创建草稿，不调用正式发表或群发接口。

### Agent 协作

- 为每篇文章建立独立 run，保存任务书、主张与来源、初稿、审稿报告、成稿、预览和草稿结果。
- 增加简单修改提案：AI 候选稿先生成 unified diff，用户可以接受或拒绝。
- 接受后候选稿复制为成稿；拒绝后保留原内容。

### Windows 交付

- 复现中文 Windows 主机上 `cp932` 控制台输出导致的 `UnicodeEncodeError`，在 CLI 边界统一
  配置 UTF-8 输出。
- 增加幂等 `install.ps1`、默认保留用户状态的 `uninstall.ps1`。
- 增加完全离线的 `scripts/demo.ps1`，一键执行 Markdown 转换、微信 HTML 校验和质量评分。
- 在隔离目录真实验证安装、运行 CLI、卸载的完整生命周期。

## 5. 验证证据

```text
上游基线测试：112 passed
当前完整测试：131 passed
Windows 生命周期：install → CLI → uninstall passed
微信真实 Canary：token → 永久封面 → draft/add passed
后台人工验收：标题、正文排版、封面正常
正式发布/群发：未执行
```

## 6. 技术栈

Python 3.11+、pytest、Markdown/HTML、YAML、Agent Skills、微信公众号 API、PowerShell、Git。

## 7. 三分钟演示脚本

1. 运行 `./scripts/demo.ps1`，展示离线预览、HTML 校验和质量评分产物。
2. 打开一个 run 的 `draft.md`、`proposal.md` 与 `proposal.json`，演示 diff、接受和拒绝。
3. 运行 `wewrite publish ... --dry-run`，展示发布计划和阻断项。
4. 展示测试结果 `131 passed`。
5. 展示公众号后台中的 Canary 草稿截图时遮挡账号标识和密钥；强调没有群发。

## 8. 面试高频问题

### 为什么选这个开源项目？

它已经有较完整的内容管道、主题和微信 API 封装，代码规模适合快速吃透。我先跑通上游测试、
画清 Prompt/Runtime/State 三层边界，再选择真实业务最需要的 Windows 交付、改稿审阅和
草稿验证作为二次开发范围。

### 最难的问题是什么？

不是调用一次微信 API，而是让本地预检、任务状态、图片处理、用户确认和真实草稿结果形成
可复现链路。Windows 中文控制台还出现“产物已经生成但命令最后崩溃”的问题，需要把编码
修复放在统一 CLI 入口，而不是逐条修改输出。

### 如何证明不是只改 README？

可以现场运行离线 Demo、完整 pytest、提案 accept/reject，并展示真实公众号草稿。新增功能
都有测试或人工验收记录。

### 有什么没完成？

目前没有统一可视化工作台，没有真实用户增长指标，也没有验证正式发表/群发。项目重点是
本地 Agent 内容管道与草稿创建，不把这些未完成项包装成现有能力。

## 9. 简历表述

- 基于 MIT 开源 WeWrite 二次开发微信公众号 Agent 工作流，完成代码审计、Windows 交付、
  可审阅改稿与草稿链路验证。
- 新增发布 Dry-run、任务产物绑定和微信 API Mock 合约测试；真实跑通 token、永久封面上传
  与 `draft/add`，并由公众号后台人工验收。
- 修复中文 Windows 控制台编码崩溃，补齐 PowerShell 安装/卸载与离线 Demo；测试由上游
  112 项扩展至 131 项。

## 10. 真实性边界

可以说“基于开源项目二次开发”“真实创建并验收公众号草稿”“补充测试至 131 项”。不能说
“项目从零开发”“已经正式发布或群发”“已有真实用户增长指标”。
